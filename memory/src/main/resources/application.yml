server:
  port: 8080
  servlet:
    context-path: /api/v1/autoscaling

management:
  server:
    port: 8081
  endpoints:
    web:
      exposure:
        include: health, info, metrics

spring:
  application:
    name: ECS-AUTO-SCALING